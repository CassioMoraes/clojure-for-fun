(ns clojure-noob.core
  (:gen-class)
  (require [clojure-noob.begginer-stuff :refer :all])
  (require [clojure-noob.clojure-errors :refer :all])
  (require [clojure-noob.hobbit-game :refer :all]))

(defn  -main
  "I don't do a whole lot ... yet."
  [& args]

  ;(bunch-of-begginer-stuff)
  ;(non-clojure-way-error-message)
  ;(clojure-way-error-message :mild)
  (println (hit))
  )